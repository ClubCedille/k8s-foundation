apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vault-tls
  namespace: vault
spec:
  secretName: vault-tls
  issuerRef:
    name: letsencrypt-etsmtl
    kind: ClusterIssuer
  commonName: vault.k8s-poc.etsmtl.club
  dnsNames:
    - vault.k8s-poc.etsmtl.club
